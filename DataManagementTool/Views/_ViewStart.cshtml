@*@{
        Layout = "_Layout";
    }*@
@{
    //HACK: Temporary solution to apply admin layout to identity
    //Should be fixed by updating each identity page to use Layout = "_AdminLayout";
    //so none admin pages use Layout = "_Layout";
    Layout = "_AdminLayout";
}

